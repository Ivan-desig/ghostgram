name: Build Signed Ghostgram (Ultimate Fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: '0'

      - name: Install Certificate
        run: |
          security create-keychain -p build build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p build build.keychain
          
          # Фикс сертификата для GitHub Actions
          openssl pkcs12 -in cert.p12 -nodes -passin pass:1 -out temp_cert.pem
          openssl pkcs12 -export -in temp_cert.pem -out cert_fixed.p12 -passout pass:1
          security import cert_fixed.p12 -k build.keychain -P 1 -A -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k build build.keychain
          
          # Раскладываем профили (создаем структуру, которую ищет скрипт)
          mkdir -p build-system/fake-codesigning/profiles
          cp profile.mobileprovision build-system/fake-codesigning/profiles/
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: Ultimate Nuke Patch
        run: |
          # 1. Удаляем требования пушей из всех ресурсов
          find . -name "*.entitlements" -exec plutil -remove "aps-environment" {} \; || true
          find . -name "*.plist" -exec plutil -remove "aps-environment" {} \; || true
          
          # 2. ПОЛНАЯ ПОДМЕНА LOGIC (Взламываем BuildConfiguration.py)
          # Вместо того чтобы править, мы просто записываем туда код-заглушку
          cat <<EOF > build-system/Make/BuildConfiguration.py
          import os

          def get_aps_environment(entitlements):
              return 'production'

          class BuildConfiguration:
              def __init__(self, arguments):
                  self.arguments = arguments
                  self.directory_path = "build-system/fake-codesigning"
                  self.team_id = "DUMMY"
                  self.bundle_id = "org.telegram.messenger"

          def resolve_codesigning(arguments, additional_codesigning_output_path=None):
              class DummyCodesigning:
                  def __init__(self):
                      self.directory_path = "build-system/fake-codesigning"
                      self.team_id = "DUMMY"
                      self.bundle_id = "org.telegram.messenger"
                  def copy_profiles_to_destination(self, destination_path):
                      print("Patch: Skipping real profile copy")
                      return None
              return DummyCodesigning()
          EOF

          # 3. Отключаем проверку версии Xcode в главном скрипте
          sed -i '' 's/if required_xcode_version != current_xcode_version:/if False:/g' build-system/Make/Make.py || true

      - name: Build App
        run: |
          BUILD_NUMBER=$(git rev-list --count HEAD)
          
          # Запускаем сборку с обходом всех проверок
          python3 -u build-system/Make/Make.py \
            --overrideXcodeVersion \
            build \
            --configurationPath="build-system/appstore-configuration.json" \
            --codesigningInformationPath=build-system/fake-codesigning \
            --configuration=release_arm64 \
            --buildNumber=$BUILD_NUMBER

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Signed-Ghostgram
          path: build/artifacts/Telegram.ipa
