name: Build Signed Ghostgram

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: '0'

      - name: Install Certificate
        run: |
          security create-keychain -p build build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p build build.keychain
          
          # Лечим и импортируем сертификат
          openssl pkcs12 -in cert.p12 -nodes -passin pass:1 -out temp_cert.pem
          openssl pkcs12 -export -in temp_cert.pem -out cert_fixed.p12 -passout pass:1
          security import cert_fixed.p12 -k build.keychain -P 1 -A -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k build build.keychain
          
          # Копируем профили
          mkdir -p build-system/fake-codesigning/profiles
          cp profile.mobileprovision build-system/fake-codesigning/profiles/
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: Nuke Push Notifications
        run: |
          # 1. Удаляем aps-environment из всех файлов .entitlements и .plist
          find . -name "*.entitlements" -exec plutil -remove "aps-environment" {} \; || true
          find . -name "*.plist" -exec plutil -remove "aps-environment" {} \; || true
          
          # 2. Чистим основной файл конфигурации сборки
          if [ -f "build-system/appstore-configuration.json" ]; then
            # Удаляем строку с aps-environment, какой бы она ни была
            sed -i '' '/aps-environment/d' build-system/appstore-configuration.json || true
          fi
          
          # 3. ПАТЧ СКРИПТА СБОРКИ (самое важное)
          # Мы заставляем Python-скрипт игнорировать отсутствие пушей в профиле
          sed -i '' "s/raise Exception('Could not find a valid aps-environment/print('SKIPPING: Could not find a valid aps-environment/g" build-system/Make/BuildConfiguration.py || true

      - name: Build App
        run: |
          BUILD_NUMBER=$(git rev-list --count HEAD)
          
          python3 -u build-system/Make/Make.py \
            --overrideXcodeVersion \
            build \
            --configurationPath="build-system/appstore-configuration.json" \
            --codesigningInformationPath=build-system/fake-codesigning \
            --configuration=release_arm64 \
            --buildNumber=$BUILD_NUMBER
            
